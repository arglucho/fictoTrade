<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="layout">
    <!-- Sidebar izquierdo -->
    <aside class="sidebar">
      <a href="/" class="menu-item">🏠 Inicio</a>
      <a href="#" class="menu-item">🔍 Explorar</a>
      <a href="#" class="menu-item">🔔 Notificaciones</a>
      <a href="#" class="menu-item">✉️ Mensajes</a>
      <a href="/perfil" class="menu-item">👤 Perfil</a>
      <a href="/logout" class="menu-item" style="color:#f44336; text-decoration:none;">❌ Cerrar Sesión</a>
    </aside>

    <!-- Feed central -->
    <main class="feed">
      <!-- Mensajes flash -->
      <% if (error && error.length > 0) { %>
        <div class="flash-message error"><%= error %></div>
      <% } %>
      <% if (success && success.length > 0) { %>
        <div class="flash-message success"><%= success %></div>
      <% } %>

      <!-- Formulario para crear objeto -->
      <div class="new-post">
        <h2>¿Qué objeto imaginario vas a crear hoy?</h2>
        <form action="/crear" method="POST">
          <input type="text" name="nombre" placeholder="Nombre" required>
          <textarea name="descripcion" placeholder="Descripción" required></textarea>
          <input type="number" name="poder" placeholder="Poder" min="0" required>
          <button type="submit">Crear</button>
        </form>
      </div>
    </main>

    <!-- Barra lateral derecha -->
    <aside class="rightbar">
      <div class="widget search-box">
        <input type="text" placeholder="Buscar">
      </div>
      <div class="widget">
        <h3>Publicidad</h3>
        <div style="background:#222;height:100px;display:flex;align-items:center;justify-content:center;">
          SPAM
        </div>
      </div>
      <div class="widget">
        <h3>Objetos Populares</h3>
        <ul>
          <li><a href="#">Capa Mágica</a></li>
          <li><a href="#">Varita Invisible</a></li>
          <li><a href="#">Mascota de Combate 3000</a></li>
        </ul>
      </div>
      <div class="widget">
        <h3>Creadores Sugeridos</h3>
        <ul>
          <li>@giovag <button>Seguir</button></li>
          <li>@leomessi <button>Seguir</button></li>
          <li>@holasoyneto <button>Seguir</button></li>
        </ul>
      </div>
    </aside>
  </div>
  <%- include('partials/footer') %>
</body>
</html>
